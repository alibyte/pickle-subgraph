type Jar @entity {
  id: ID!
  token: Bytes!
  ratio: BigInt!
  jarBalance: BigInt!
  totalSupply: BigInt!
  available: BigInt!
  tokenZero: Bytes!
  tokenOne: Bytes!
  tokenZeroCumulative: BigInt!
  tokenOneCumulative: BigInt!
  timestamp: BigInt!
}

type Account @entity {
  id: ID!
  balance: BigInt!
  staked: BigInt!
  stakingRewards: BigInt!
}

type JarBalance @entity {
  id: ID!
}

type RewardContract @entity {
  id: ID!
  currentRewards: BigInt!
  totalRewards: BigInt!
  stakedTokens: BigInt!
}
